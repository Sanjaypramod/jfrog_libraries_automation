from setuptools import setup, find_packages

setup(
    name="multiply_lib",
    version="0.1.0",
    author="Your Name",
    author_email="poc@example.com",
    description="A Python library for multiplication operations",
    packages=find_packages(),
    install_requires=[],  # Add dependencies if needed
)